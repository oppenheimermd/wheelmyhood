@model IEnumerable<WheelMyHood.Models.Region>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutConsole.cshtml";
}
@section DefaultHeader{
    <ul class="uk-breadcrumb">
        <li><span href="">Home</span></li>
    </ul>
}


<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="uk-table uk-table-divide uk-table-hover">
    <thead>
        <tr>
            <th>
                Continent
            </th>
            <th>
                Region code
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ContinentName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RegionCode)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.RegionCode">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.RegionCode">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.RegionCode">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

